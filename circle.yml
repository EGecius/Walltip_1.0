#
# Build configuration for Circle CI
#

general:
    artifacts:
        - /home/ubuntu/Demo_CircleCI_Android/app/build/outputs/apk/

machine:
    environment:
        ANDROID_HOME: /usr/local/android-sdk-linux
    java:
        version: openjdk8

checkout:
   post:
        - echo "Egis says ECHO!!!"
        - mkdir $CIRCLE_ARTIFACTS/img
#        - curl "http://weknowyourdreams.com/images/world/world-05.jpg" \
#            > $CIRCLE_ARTIFACTS/img/world.jpg
        - curl http://udacity.github.io/ud595-shell/stuff.zip -o $CIRCLE_ARTIFACTS/things.zip
        - cp -r licenses/. $ANDROID_HOME/licenses

dependencies:
    pre:
        - echo yes | android update sdk --no-ui --all --filter build-tools-25.0.2
        - echo yes | android update sdk --no-ui --all --filter extra-android-m2repository
        - echo yes | android update sdk --no-ui --all --filter extra-google-m2repository

test:
    override:

#        - echo "<p>Hello, <img width=80 src='img/world.svg'></p>" \
#              > $CIRCLE_ARTIFACTS/hello.html
#        - (./gradlew assemble test lint -PdisablePreDex):
        - (./gradlew assemble lint -PdisablePreDex):
            timeout: 600
#        Wrote HTML report to file:///home/ubuntu/Walltip_1.0/app/build/reports/lint/lint-result.html
        - cp -r file:///home/ubuntu/Walltip_1.0/app/build/reports/ $CIRCLE_ARTIFACTS

#        - emulator -avd circleci-android22 -no-audio -no-window:
#            background: true
#            parallel: true
#        - circle-android wait-for-boot
#
#        - adb install /home/ubuntu/Demo_CircleCI_Android/app/build/outputs/apk/app-debug.apk
#
#        - (./gradlew connectedAndroidTest):
#            timeout: 360
#
